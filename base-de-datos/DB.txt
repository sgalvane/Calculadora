use ejemplo;
create table citas ( 
id_cita INT NOT NULL auto_increment, 
id_paciente INT NOT NULL, medico VARCHAR (30) NOT NULL, 
fecha_cita DATE NOT NULL, 
fecha_eliminacion DATE, 
primary key (id_cita) 
);	

